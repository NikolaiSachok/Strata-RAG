# atlas-orchard

> Engineering README — build instructions only. NOISE for a content corpus. The product
> description lives in `docs/description.docx` and the spec in `docs/prd.md`.

## Run
`flutter run`
