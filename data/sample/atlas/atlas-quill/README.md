# atlas-quill

> Build README only. This project ships NO product/theme content doc (no description,
> no PRD, no overview) — only this engineering README. It exists so the coverage report
> flags it as a BLIND SPOT (zero content docs).

## Run
`flutter run`
