# atlas-ledger

> Engineering README for the Atlas Ledger project. Build/setup instructions only — no
> product or theme description here. This is NOISE for a content-focused corpus; the
> real product content lives in `docs/description.docx`.

## Setup
```
flutter pub get
flutter run
```

## Structure
- `back/` — backend stubs
- `docs/` — product descriptions (.docx)
